---

remi_mirror_cron_disabled: true
remi_mirror_source: 'rsync://repo1.ash.innoscale.net/remi/'
remi_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
remi_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
